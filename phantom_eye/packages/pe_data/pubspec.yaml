name: pe_data
description: Pure-Dart network clients and response parsers for Phantom Eye. No Flutter, no map engine.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  http: ^1.6.0
  pe_core: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
